import React from 'react'
import ReactDOM from 'react-dom/client'
import GoalChain from './App.jsx'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <GoalChain />
  </React.StrictMode>,
)
